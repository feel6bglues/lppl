[pytest]
testpaths = tests/unit tests/contract
markers =
    slow: marks tests as slow (typical backtest, ~15s+)
    smoke: marks tests as CLI end-to-end smoke tests
    tdx: requires TDX_DATA_DIR environment variable
